# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# Virtual environments
venv/
.venv/
env/

# Never commit model weights / caches
*.safetensors
*.bin
*.gguf
.cache/

# Generated run artifacts (regenerated by demos / tests / probes)
metrics/
agent_images/
agent_audio/
svg_out/
generated_workflows.json
*.wav
*.png
*_probe.json

# MITM CA material — the private key must NEVER be committed (regenerate with
# `make mitm-ca`). Both cert and key are host-local and gitignored together.
sandbox/mitm/

# GeneralUser GS soundfont (32 MB binary; re-fetch with `make soundfont`)
sandbox/GeneralUser-GS.sf2

# Editor / OS
.vscode/
.idea/
.DS_Store
